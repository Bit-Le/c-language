下面程序实现交换调用它的两个整型变量x和y的值，在程序划线处填入相应的内容。
#include <stdio.h>
void swap(             ) 
{
int temp;
temp=*x;*x=*y;*y=temp;
}
int main(void)
{
int a,b;
scanf("%d%d",&a,&b);
swap(             );
printf("a=%d,b=%d\n",             );
    return 0;
}
