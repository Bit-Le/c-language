下面程序按字母顺序比较两个字符串s1和s2的大小，若相等，则输出0；否则输出其第一个不相等的字符的ASCII差值。
#include <stdio.h>
int main()
{ 
char c1[30],c2[30],*s1=c1,*s2=c2;
int i;
gets(s1);
________________;
while(*s1!='\0' &&____________)
{
s1++;
s2++;
}
if(*s1=='\0' &&______________-)
i=0;
else
i= _______________;
printf("差值=%d\n",i); 
    return 0; 
}
