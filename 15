使用“scanf("a=%d,b=%d",&a,&b)”,要使a,b的值均为25,正确的输入是( )。
A. a=25,b=25
B. 25 25(空格分开)
C. 25,25
D. a=25 b=25(空格分开)

正确答案: A:a=25,b=25;




写出下列程序运行结果__________。
#include <stdio.h>
void main()
{
       int x,y;
       x=-1;
       if(x++<0) y=-1;
       else if(x==0) y=0;
       else y=1;
       printf("x=%d,y=%d\n",x,y);
}

正确答案：
(1) x=0,y=-1





结构化程序设计由3种基本结构组成,3种基本结构组成的算法( )。
A. 能完成符合结构化的任务
B. 只能完成一些简单的任务
C. 只能完成部分复杂的任务
D. 可以完成任何复杂的任务

正确答案: D:可以完成任何复杂的任务;
